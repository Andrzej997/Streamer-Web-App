import { PasswordInputDirective } from './password-input.directive';

describe('PasswordInputDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordInputDirective();
    expect(directive).toBeTruthy();
  });
});
